# Native Skills

Place dotfiles-owned Skills in direct child directories of this folder. Each
directory must contain `SKILL.md`, and its directory name must match the
frontmatter `name`. Native Skills are copied unchanged into the final flat
installation and do not receive a source prefix.
