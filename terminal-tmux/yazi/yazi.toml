# Yazi 默认在未设置 EDITOR 时用 vi 打开文本文件，但 Vim 的 mouse 选项默认
# 为空，滚轮事件不会用于滚动文件内容。仅为 Yazi opener 启用全鼠标模式，
# 保留 Enter 后直接编辑文件的原有行为。
[opener]
edit = [
	{ run = 'vi -c "set mouse=a" %s', block = true, desc = "vi with mouse", for = "unix" },
]
